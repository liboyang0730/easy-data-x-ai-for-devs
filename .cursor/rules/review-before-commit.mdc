---
description: 所有改动须经作者 review 后再提交
globs: 
alwaysApply: true
---

# Git 提交流程

- **提交前必须展示 diff**：执行 `git add`/`commit`/`push` 前，必须先展示改动内容的 diff，等作者确认。
- **不要自动提交**：完成代码或文档改动后，不要自动执行 `git add`、`git commit`、`git push`。
- **等待明确同意**：作者 review diff 并明确同意（如说「提交」「可以提交」「push」等）后，再执行提交相关命令。
